const express = require('express');
const router = express.Router();

// Placeholder for Phase 3 - Router LLM Brain
// This will be implemented in Phase 3

router.post('/', (req, res) => {
  res.status(501).json({ error: 'Not implemented yet - coming in Phase 3' });
});

module.exports = router;
